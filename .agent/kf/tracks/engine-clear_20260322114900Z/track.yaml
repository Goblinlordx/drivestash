id: engine-clear_20260322114900Z
title: Add clear() method to SyncEngine
type: feature
status: pending
created: '2026-03-22'
updated: '2026-03-22'
spec:
  summary: 'Expose clear() on SyncEngine for user logout/account switch. Delegates to LocalStore.clear(), resets remoteFileId
    and pending sync state, sets status to idle.

    '
  context: 'When a user logs out, the next user must not see stale data from the

    previous session. LocalStore.clear() exists internally but is not

    exposed through the SyncEngine public API. This is critical for

    multi-user or account-switching scenarios.

    '
  codebase_analysis: "- LocalStore.clear() exists at src/local-store.ts:72\n- SyncEngine interface (src/sync-engine.ts:25-35)\
    \ has no clear()\n- LocalStorePort interface (src/sync-engine.ts:8-14) has no clear()\n- Internal state to reset: remoteFileId,\
    \ syncInProgress, pendingSync\n- destroy() already clears listeners — clear() should NOT destroy\n  the engine, just wipe\
    \ data and reset state\n"
  acceptance_criteria:
  - clear() added to SyncEngine interface
  - clear() added to LocalStorePort interface
  - clear() wipes all local records via LocalStore.clear()
  - clear() resets remoteFileId to null
  - clear() sets status to idle
  - clear() cancels any pending sync (does not interrupt in-progress)
  - Unit tests cover all above behaviors
  - README API Reference updated with clear() method
  - README Integration Guide includes logout/clear usage
  - CHANGELOG updated with v0.2.0 entry
  out_of_scope: '- Deleting the remote Drive file (clear is local-only)

    - drivestash-events integration (separate repo)

    '
  technical_notes: 'clear() should be safe to call at any time. If a sync is in progress,

    clear() should wait for it to finish (or let it finish and then clear).

    After clear(), the engine is in a fresh state — next push creates a

    new remote file.

    '
plan:
- phase: Interface
  tasks:
  - text: Add clear() to SyncEngine interface
    done: true
  - text: Add clear() to LocalStorePort interface
    done: true
- phase: Implementation
  tasks:
  - text: Implement clear() in createSyncEngine
    done: true
- phase: Tests
  tasks:
  - text: Test clear wipes all records
    done: true
  - text: Test clear resets remoteFileId (next push creates new file)
    done: true
  - text: Test clear sets status to idle
    done: true
  - text: Test clear while sync in progress
    done: true
- phase: Docs
  tasks:
  - text: Add clear() to README API Reference
    done: true
  - text: Add clear() to README Integration Guide with logout example
    done: true
  - text: Update CHANGELOG with v0.2.0 entry
    done: true
extra: {}
